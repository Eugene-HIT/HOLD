# 2026-07-05 指部详情页图表门控误判修复

## 任务名称
修复指部 PPG 详情页已经拿到完整主动检测波形数据但图表区域仍完全不显示的问题。

## 修改文件
- web/pages/active-report/index.js

## 主要变更
- 页面构建标记提升为 `active-report 2026-07-05-06`
- 删除“必须本页亲眼见过 `FINGER_PPG_ACTIVE_TEST` 开始后才显示图表”的错误门控
- 图表显示条件改为：当前 `viewReport` 只要已经带有主动检测波形或跳点数据，就立即显示并触发绘图
- 调试信息补充 `wave` 与 `marker` 计数，便于直接确认详情页当前到底有没有拿到可绘图数据

## 本次判断与结论
- 用户真机截图已经证明：详情页不是没有记录，而是已经拿到 `preview=1 canGenerate=1 beats=32 rr=31` 的完整预览对象
- 同时页面摘要明确显示“当前实时点数 1500，窗口分片 1/1”，说明波形数据本身已经存在
- 因此问题不是绘图函数没搬过去，而是显示门控条件被错误写成了“这个页面必须先见到检测开始”
- 重新进入详情页查看刚完成的记录时，这个条件通常为假，所以图表整块被 `wx:if` 隐藏，表现成“什么都没有”

## 验证结果
- `node --check web/pages/active-report/index.js` 通过
- VS Code 问题诊断通过：
  - web/pages/active-report/index.js

## 待确认事项
- 需要用户真机确认：当前重新进入指部详情页后，图表面板是否已经恢复显示

## 下一步建议
- 若图表面板已经显示但曲线仍不对，再继续排查数据源本身，而不是显示门控