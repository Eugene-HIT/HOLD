# 2026-07-05 胸口VOFA追加心跳间隔输出

## 任务名称
为独立胸口 VOFA 小工程追加“上一拍到本拍的心跳间隔”输出，单位 ms。

## 修改文件
- src/max30102_xiao_esp32s3_chest_vofa/main.cpp

## 主要变更
- 胸口 VOFA 串口帧从 12 列扩展为 13 列
- 在现有输出末尾追加 `lastBeatIntervalMs`
- 该值直接来自 `heartRateEstimator.lastBeatIntervalMs()`，表示上一拍到当前拍的间隔毫秒数
- 前 12 列顺序保持不变，避免影响你当前已经在看的波形列

## 验证结果
- 待独立 env 编译验证

## 待确认事项
- 需要在 VOFA 中确认新增列是否已出现在末尾，并观察数值是否与当前胸口 beat 节律一致

## 下一步建议
- 若还要更直接排查 beat 识别，可下一步再追加 `beatCount` 与 `hasValidBpm` 的独立列做对照