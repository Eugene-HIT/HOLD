# 2026-05-22 Agent 配置记录

## 任务名称
创建代码开发专用 custom agent

## 修改文件
- .github/agents/code-architect.agent.md

## 主要变更
- 新增一个工作区级 custom agent
- 约束该 agent 以工程结构清晰、模块职责明确、中文完整注释、主动资料检索、及时日志记录为核心工作方式
- 明确要求在不确定时先提出问题，而不是猜测实现
- 明确要求每次生成代码后进行简洁说明和最小必要验证

## 实现思路
- 采用 VS Code Copilot custom agent 方式落地
- 将行为要求写入 agent 主体说明，便于后续直接从 agent 选择器调用
- 工具范围包含 read、edit、search、web、execute、todo，满足工程开发、资料检索、验证与记录需求

## 待确认事项
- 该 agent 是否只用于当前工作区，还是后续还要迁移为用户级通用 agent
- 头部注释格式是否需要进一步统一为固定模板
- log 子目录命名是否按模块固定，例如 firmware、sensors、system、docs

## 验证结果
- .github/agents 目录已创建
- code-architect.agent.md 已成功写入
- 日志文件已写入 log/system

## 下一步建议
1. 在 VS Code 中重新加载窗口或重新打开聊天面板，确认 agent 是否出现在选择列表中
2. 若希望跨项目复用，可再复制一份到用户级 prompts/agents 目录
3. 后续可以继续补充一个配套的 instructions 文件，专门约束注释模板和 log 模板
