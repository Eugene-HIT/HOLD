# 2026-05-23 dev 开发文档规范与 MAX30102 原始读取要求

## 任务名称
新增开发专用文档目录规范，并产出 MAX30102 原始数据读取开发要求文档

## 修改文件
- log/README.md
- .github/prompts/create-dev-doc-and-log.prompt.md
- log/dev/2026-05-23/max30102/raw-read/MAX30102原始数据读取开发要求.md

## 主要变更
- 为 log 目录补充 `dev` 二级目录的用途说明与推荐层级结构
- 将“临时开发文档应放入 log/dev/日期/模块/功能”规则写入统一文档输出 prompt
- 为 plan agent 增强文档输出 prompt，使其更偏开发前要求文档口径
- 新增一份面向当前工程的 MAX30102 原始数据读取开发要求文档，作为后续编码的直接约束依据

## 实现思路
- 先沿用现有 log / prompt 体系，不新建独立模板体系，避免结构继续分叉
- 将目录规范写到 log 说明和文档输出 prompt 两处，确保后续人工与 agent 调用都能看到
- 将 MAX30102 当前阶段目标严格限定为“读到原始 Red / IR 数据”，避免和后续心率、血氧阶段混淆

## 待确认事项
- MAX30102 当前准备采用直接寄存器配置，还是先走成熟库打通链路
- XIAO ESP32S3 当前项目最终固定的 I2C 引脚定义是否需要写入上位开发指南
- 后续是否还要为 `log/dev` 增加一个固定 Markdown 模板

## 验证结果
- 新文档已按 `log/dev/日期/模块/功能/文档.md` 结构写入
- 统一文档输出 prompt 已补充 plan agent 使用口径与 `log/dev` 路径要求
- log/README 已补充 `dev` 子目录说明

## 下一步建议
1. 直接按新开发要求文档开始写 MAX30102 原始数据读取代码
2. 首轮优先做 I2C 扫描与地址确认，再做 FIFO 原始数据输出
3. 如果后续这类文档会频繁出现，可继续补一个 `log/dev` 固定模板 prompt