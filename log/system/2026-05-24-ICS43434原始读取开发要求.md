# 2026-05-24 ICS43434 原始读取开发要求

## 任务名称
新增 ICS43434 数字麦克风最小接入开发要求文档

## 修改文件
- log/dev/2026-05-24/ics43434/raw-read/ICS43434原始数据读取开发要求.md

## 主要变更
- 新增一份面向当前工程的 ICS43434 原始数据读取开发要求文档
- 将今晚的开发目标严格限定为“确认 I2S 音频样本是否能被稳定读取并通过串口观察到变化”
- 明确 ICS43434 属于 I2S 数字麦克风接入，不允许退化为模拟量读取路线
- 预先定义首轮建议接线、串口输出范围、验收标准和待确认事项

## 实现思路
- 先沿用现有 `log/dev/日期/模块/功能` 的开发文档结构，不新开一套单独模板
- 先把问题收紧到“读到样本”而不是“理解音频内容”，避免今晚任务范围膨胀
- 将串口输出要求限制为少量窗口统计值，保证肉眼可判断且不刷屏
- 先给出建议 I2S 引脚映射，后续编码时再依据实物接线和运行结果确认最终脚位

## 待确认事项
- 当前实物板丝印与文档中的 `BCLK / WS / DOUT / SEL` 是否一一对应
- 首轮编码时采用的 I2S Arduino 接口版本
- SEL 固定到 GND 后是否与首轮单声道配置匹配

## 验证结果
- 新文档已按 `log/dev/日期/模块/功能/文档.md` 结构写入
- 文档已包含目标、范围、输入输出、约束、接线建议、验收标准和参考来源
- 文档已明确将本阶段目标限定为最小可观测验证，不扩展到录音、频谱或识别算法

## 下一步建议
1. 按文档进入 ICS43434 最小 I2S 读取实现
2. 首轮仅输出样本统计值和读取状态，不增加额外提示
3. 完成运行验证后，再补一份模块总结文档记录接线表和串口判读样例