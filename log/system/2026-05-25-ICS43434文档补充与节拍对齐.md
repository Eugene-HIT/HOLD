# 2026-05-25 ICS43434 文档补充与节拍对齐

## 任务名称
补充 ICS43434 开发要求文档中的接线说明，并将麦克风更新节拍与当前系统节拍对齐

## 修改文件
- log/dev/2026-05-24/ics43434/raw-read/ICS43434原始数据读取开发要求.md

## 主要变更
- 将开发要求文档的最后更新时间更新为 2026-05-25
- 补充 ICS43434 常见丝印与 XIAO ESP32S3 引脚对应关系，降低接线歧义
- 将麦克风模块的节拍要求从宽泛描述收敛为：内部批处理优先按 10ms 更新，串口状态默认按 500ms 输出一行
- 明确该节拍是为了对齐当前工程中板载灯和心率状态行的现有节奏，便于后续多模块联调

## 实现思路
- 先以当前固件中的实际节拍作为依据，而不是沿用模糊表述
- 当前已确认的固件节拍为：板载用户灯每 250ms 翻转一次，心率状态日志每 500ms 输出一行，MAX30102 轮询节拍为 10ms
- 因此麦克风文档中将“内部更新”和“串口打印”拆开定义：内部统计更新按 10ms 对齐主循环轮询，串口状态打印按 500ms 对齐心率与板载灯完整明灭周期

## 待确认事项
- 后续编码时最终使用的 I2S API 版本
- 实物模块的声道选择引脚在接地和接 3V3 两种情况下的实际表现
- 首轮实现中每个 10ms 批次读取的样本数量是否需要随采样率微调

## 验证结果
- 开发要求文档已补充接线对照表、节拍约束和验收口径
- 文档中关于串口输出频率的描述已从区间值收敛为默认 500ms
- 文档已明确内部 10ms 更新与外部 500ms 打印的关系

## 下一步建议
1. 直接按更新后的文档进入 ICS43434 最小 I2S 读取实现
2. 固件中把样本统计窗口和串口状态行先做成可直接对照的固定格式
3. 首轮跑通后再补一份模块总结文档，记录最终接线表和串口样例