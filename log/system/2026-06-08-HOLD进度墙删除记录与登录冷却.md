# 2026-06-08 HOLD 进度墙删除记录与登录冷却

## 任务名称
HOLD 进度墙补充删除记录与邮箱发送冷却

## 修改位置
- web/hold-progress-wall/src/components/progress-wall-app.tsx
- web/hold-progress-wall/src/components/auth-panel.tsx
- web/hold-progress-wall/src/components/record-detail-panel.tsx

## 本次完成内容
- 为记录详情侧栏补充删除记录按钮
- 新增 progress_records 删除流程，并在删除后尝试清理 Supabase Storage 中的关联图片
- 为 Magic Link 登录发送增加 60 秒前端冷却
- 将 Supabase 邮件 rate limit 报错改为更容易理解的提示文案

## 关键实现说明
- 删除逻辑放在主应用状态层处理，避免把数据库操作散落到展示组件中
- 图片清理通过解析公开图片 URL 对应到 record-images 桶内路径后批量删除
- 发送冷却采用本地倒计时方式，优先解决重复点击触发的限流问题
- 当前提示中已明确：出现 rate limit 不代表必须额外购买服务，先等待冷却结束即可

## 验证结果
- npm run lint：通过
- npm run build：通过

## 待确认事项
- 当前 60 秒冷却为前端限制，若后续要做更严格控制，可再叠加服务端侧节流策略
- 若后续需要“删除前二次弹层确认”或“回收站”机制，可单独追加

## 下一步建议
1. 本地验证删除记录后时间轴与详情侧栏是否同步更新
2. 线上验证邮件发送按钮是否在发送后进入 60 秒冷却
3. 若后续成员变多，再评估自定义 SMTP 或邮件服务商接入