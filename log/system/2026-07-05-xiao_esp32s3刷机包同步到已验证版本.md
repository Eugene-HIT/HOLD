# 2026-07-05 XIAO ESP32S3 刷机包同步到已验证版本

## 任务名称
将 `xiao_esp32s3_integrated_hold` 的 Windows 一键刷机包同步到当前本机已验证可启动的固件版本。

## 触发原因
- 用户询问此前生成的刷机包是否仍可正常使用
- 核对发现当前 `.pio/build/xiao_esp32s3_integrated_hold/firmware.bin` 与发布包内 `firmware.bin` 大小不同

## 关键判断
- 当前已验证构建产物 `firmware.bin` 大小为 `966080` 字节
- 旧刷机包中的 `firmware.bin` 大小为 `965488` 字节
- 说明旧刷机包不是当前这一版，继续分发会带来版本歧义

## 处理动作
- 同步发布目录中的 `bootloader.bin`
- 同步发布目录中的 `partitions.bin`
- 同步发布目录中的 `firmware.bin`
- 更新 `README.txt` 版本说明
- 重打包 zip 发布物

## 验证结果
- 已确认发布目录中的 `firmware.bin` 与当前构建产物大小一致
- 已重新生成 zip 刷机包

## 下一步建议
1. 以后每次实板验证后再打包，避免“包能刷但不是当前版本”的歧义
2. 后续可在包名中加入日期或版本号